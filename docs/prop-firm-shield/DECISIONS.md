# Decisions log

Working decisions for the guard. Each names the date, what was decided, why, and what
would reopen it. Product rules in `TOTAL-BRIEF-Prop-News-Guard.md` still win.

| # | Date | Decision | Why | Reopens if |
|---|---|---|---|---|
| D1 | 23 Sep 2026 | Push is primary. Every rung is also mirrored as a local OS notification, deduped by alert id. | No push channel is guaranteed. APNs coalesces, FCM throttles, Doze delays. A quiet single ping is the failure mode the brief names. | Never. Both are cheap. |
| D2 | 23 Sep 2026 | Working name: Wideberth. Runner-up: Keepclear. | Grok's three all fail. Holdoff is a live App Store finance app at the same price. Bench is Bench Accounting. Still is unsearchable. Wideberth is clean on both stores, ownable, and the idiom means exactly what the product does. | A trademark search at UKIPO, EUIPO and USPTO finds a live mark in class 9, 36 or 42. |
| D3 | 23 Sep 2026 | Docs stay in mtbridge-releases until the name clears, then move to a private repo named after the product. | The brief says invisible to the firm and never on a terminal. Living in a public MTBridge repo contradicts that. Creating a repo before the name is checked means renaming it later. | Name clears. Move the same week. |
| D4 | 23 Sep 2026 | The Windows gate ships as its own tray app, not inside the MTBridge agent. | Same reason as D3. MTBridge is a terminal-side connector. | Never. |
| D5 | 23 Sep 2026 | Android foreground detection uses UsageStats, not AccessibilityService. | Play rejects accessibility use that isn't for accessibility. Focus apps that lean on it get pulled. | Google publishes a policy carve-out for this use. |
| D6 | 23 Sep 2026 | The Android gate service is short-lived: an exact alarm starts it at window open, it stops at window end. No always-on service. | Persistent services are what OEM battery managers kill. A service that runs ten minutes a few times a day survives, and it's easier to justify to Play. | Windows get so frequent that start-up latency matters. |
| D7 | 23 Sep 2026 | The iOS gate is Apple's Screen Time shield with a "View for 60 seconds" secondary button. No hold-to-view on iOS. | The shield card is Apple's template. Hold gestures aren't available on it. | Apple opens custom shield UI. |
| D8 | 23 Sep 2026 | Shared app in Flutter. Native code only for the gate hooks and the iOS Screen Time extensions. | Flutter ships Android, iOS, Windows and macOS from one tree and handles tablet layouts well. React Native for Windows is a second-class target. | The team has deep React Native experience and none in Flutter. |
| D9 | 23 Sep 2026 | Backend in Laravel with Horizon queues, Postgres and Redis. | It's the stack Karnek already runs in production, so no new ops. Push scheduling is delayed queue jobs, which Horizon does well. | The team prefers a TypeScript service. Nothing in the design depends on PHP. |
| D10 | 23 Sep 2026 | Calendar vendor shortlist: Trading Economics and FXMacroData. Quotes from both, a two-week side-by-side before signing. | Trading Economics is flat rate, FXMacroData is per user. Which is cheaper depends on how many free users turn up. Neither scrapes. | Quotes come back above the ceiling in `CALENDAR-VENDORS.md`. |
| D11 | 23 Sep 2026 | Windows ships as MSIX signed with the existing code-signing certificate. Sideload first, Store later. | MSIX gives clean install, updates and, with Store identity, WNS push. The certificate already exists for MTBridge installers. | The certificate can't be reused across products. |
| D12 | 23 Sep 2026 | Free tier gets the full ladder and the local mirror. Nothing about being woken up is Pro. | Free has to stop the breach or the product has no reason to exist. Pro sells precision, not safety. | Never. |
| D13 | 23 Sep 2026 | Four App IDs registered with Family Controls (Development): com.stanchion.wideberth plus .monitor, .shieldconfig, .shieldaction. The distribution entitlement is not yet requested. Apple has replaced the per-app form with a single "Get Entitlement" button that accepts the Family Controls terms for the whole team (Stanchion Systems Ltd). Jamie presses it. | The terms fit: primary purpose is letting individuals manage their own device use, no managing other adults' devices, no sharing usage data. Team-level acceptance means a later rename does not need refiling. | Apple changes the flow again. |
